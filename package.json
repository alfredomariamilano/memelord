{
  "name": "memelord",
  "version": "0.1.5",
  "description": "Persistent memory system for coding agents with reinforcement learning. Powered by Turso.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "bin": {
    "memelord": "dist/cli.mjs"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "ts": "bunx tsc --noEmit",
		"biome:check": "bunx -y @biomejs/biome check",
		"biome:lint": "bunx -y @biomejs/biome lint",
		"biome": "bun run biome:check && bun run biome:lint",
		"biome:fix": "bun run biome:check -- --fix && bun run biome:lint -- --fix",
		"lint": "bun run ts && bun run biome",
		"lint:fix": "bun run ts && bun run biome:fix",
    "build": "tsc -p tsconfig.build.json && bun build packages/cli/src/index.ts --target node --external @tursodatabase/database --external @modelcontextprotocol/sdk --external zod --external @huggingface/transformers --outfile dist/cli.mjs && node -e \"let f=require('fs');let c=f.readFileSync('dist/cli.mjs','utf8');f.writeFileSync('dist/cli.mjs',c.replace('#!/usr/bin/env bun','#!/usr/bin/env node'))\"",
    "prepublishOnly": "bun run build"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "@tursodatabase/database": "^0.6.1",
    "@huggingface/transformers": "^4.2.0",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@biomejs/biome": "2.4.15",
    "@types/node": "^22.19.19",
    "bun-types": "latest",
    "typescript": "^6.0.3"
  },
  "workspaces": [
    "packages/*"
  ],
  "keywords": [
    "mcp",
    "ai",
    "agent",
    "memory",
    "coding-agent",
    "turso",
    "vector-search",
    "reinforcement-learning"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/glommer/memelord"
  },
  "license": "MIT"
}
